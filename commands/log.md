---
description: Show the full message history with a peer
argument-hint: <peer>
---

Print the complete conversation with a peer, oldest first:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/bridger" log $ARGUMENTS
```

Show the output to the user as-is; it is already human-readable (seq, timestamp, direction, type, body).
